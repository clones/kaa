#!/usr/bin/python

import sys
import kaa.thumb

for file in sys.argv[1:]:
    thumbnail = kaa.thumb.check(file)
    if thumbnail:
        print '%s: %s' % (file, thumbnail)
    else:
        print 'thumbnailing %s' % file
        kaa.thumb.create(file)
